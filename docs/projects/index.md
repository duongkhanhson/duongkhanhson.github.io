---
title: Projects
---

# Projects

This page highlights the main portfolio projects, ready for GitHub Pages under the `docs/` source folder.

## Featured Project

### Phone Camera Face Recognition UI
- Folder: `phone-camera-face-recognition`
- Description: Python desktop app that connects to a mobile phone camera stream and performs real-time face detection.

## Other Projects

- `esp32_cam_control/` — ESP32 camera streaming and control.
- `esp32_cam_face_gui.py` — ESP32 camera stream viewer with GUI.
- `servo_control/` and `servo_gui.py` — servo control hardware and Python interface.
- `pid_motor_serial/` and `pid_motor_serial_gui.py` — PID motor serial control.
- `green_led_control/` — LED control project.
- `hand_detect.py`, `face_detect.py`, `eye_detection.py` — computer vision demos.

## Notes

If GitHub Pages source is set to `docs/`, this page will appear at `/projects/`.
